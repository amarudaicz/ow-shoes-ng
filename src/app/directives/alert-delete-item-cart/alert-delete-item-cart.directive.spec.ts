import { AlertDeleteItemCartDirective } from './alert-delete-item-cart.directive';

describe('AlertDeleteItemCartDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertDeleteItemCartDirective();
    expect(directive).toBeTruthy();
  });
});
